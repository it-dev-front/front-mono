import { Navigation } from "@/widgets/navigation";

export default function WithGNBOnlyLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <>
      <Navigation />
      <main>{children}</main>
    </>
  );
}
